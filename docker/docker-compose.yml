version: '3.8'

services:
  osint-bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: osint-bot
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - ../logs:/app/logs
      - ../data:/app/data
